import React from 'react'
import Hero from '@components/Hero';

const Home: React.FC = (): JSX.Element => {
    return (
        <div>
            <Hero />
        </div>
    );
};

export default Home
